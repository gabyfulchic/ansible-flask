---
driver:
  name: docker
lint:
  name: yamllint
  options:
    config-file: .yamllint
platforms:
  - name: docker_centos7
    image: jrei/systemd-centos:7
    privileged: true
provisioner:
  name: ansible
  playbooks:
    converge: ../../converge.yaml
  inventory:
    links:
      host_vars: ../../host_vars
  lint:
    name: ansible-lint
  log: true
scenario:
  name: default
verifier:
  name: testinfra
  directory: ../../tests
  lint:
    name: flake8
